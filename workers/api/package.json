{
  "name": "tennis-admin-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Workers API for tennis admin system",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create tennis-admin-db",
    "d1:migrate": "wrangler d1 execute tennis-admin-db --file=../../db/migrations/0001_players.sql && wrangler d1 execute tennis-admin-db --file=../../db/migrations/0002_stringing.sql && wrangler d1 execute tennis-admin-db --file=../../db/migrations/0003_inventory.sql && wrangler d1 execute tennis-admin-db --file=../../db/migrations/0004_history.sql"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
