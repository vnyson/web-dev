{
  "name": "tennis-stringing-site",
  "version": "1.0.0",
  "private": true,
  "description": "Tennis Stringing & Racket Services - static site",
  "scripts": {
    "build": "npm run copy-css",
    "copy-css": "mkdir -p css/design-system && cp ../../packages/design-system/css/*.css css/design-system/",
    "dev": "echo 'Open index.html in browser'",
    "lint": "echo 'No linting configured'"
  },
  "dependencies": {
    "@vnyson/design-system": "workspace:*"
  }
}
